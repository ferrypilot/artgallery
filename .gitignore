node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# 키가 들어 있습니다. 절대 커밋하지 마세요.
.env.local

# 학생 작품 원본. 용량이 큽니다.
public/works/*
!public/works/.gitkeep

# npm run backup 결과물. 영상이 통째로 들어 있습니다.
backup/

# 2FA 복구 코드·비밀번호 메모. 절대 커밋하지 마세요.
# 한 번 올라간 비밀값은 지워도 이력에 남습니다.
docs/recovery-codes.txt
*recovery-code*
*.secret

# 배경 그림 원본(9MB). 배포에 쓰는 것은 public/school-bg.jpg 로 줄인 것이고,
# 원본은 저장소에 넣지 않습니다 — 한 번 들어가면 이력에서 지워지지 않습니다.
# 다시 만들 일이 있으면 구글 드라이브에서 가져오세요.
docs/온라인학교배경.png
