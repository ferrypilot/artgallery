# 갈래 1 — 서버 없이 전시 열기

Supabase 도 API 키도 없이, 폴더 하나로 전시를 엽니다.
선생님이 Grok 앱에서 직접 영상을 만들고, 파일을 폴더에 넣는 방식입니다.

## 폴더 구조

```
전시/
├─ exhibition.html          ← 전시장
├─ theme.css                ← 글꼴·색 (없으면 화면이 풀립니다)
├─ school-bg.jpg            ← 로비 바탕 그림 (없으면 로비만 허옇게 뜹니다)
├─ exhibition.json          ← 작품 목록
└─ works/                   ← 손으로 만들 때. npm run backup 은 students/ 를 씁니다
   ├─ seoyeon-01.mp4
   ├─ seoyeon-02.mp4
   └─ doyoon-01.mp4
```

**이 폴더는 이미 `public/` 으로 있습니다.** 통째로 복사해서 어디든 두면
그대로 전시가 됩니다. Next.js 도 Supabase 도 필요 없습니다.

`theme.css` 는 사이트의 글꼴·색을 정하는 파일입니다(`public/theme.css`).
`exhibition.html` 옆에 없으면 색과 글꼴이 통째로 풀립니다 — `npm run backup`
은 자동으로 같이 담습니다. 손으로 폴더를 만들 때만 챙기세요.
안에서 부르는 구글 폰트는 인터넷이 없으면 조용히 실패하고 맑은 고딕으로
떨어집니다. 화면은 그대로 읽힙니다.

## 여는 방법

**HTML 파일을 더블클릭하지 마세요.** `file://` 로 열면 브라우저가
보안상 목록 파일 읽기를 막아서, 내장 예시만 뜨고 실제 작품이 안 보입니다.

폴더에서 터미널을 열고

```bash
npx serve .
```

http://localhost:3000 으로 접속하면 됩니다. 종료는 `Ctrl+C`.

Python 이 편하시면 `python3 -m http.server 8000` 도 같습니다.

## exhibition.json 쓰는 법

한 학생이 전시장 하나입니다.

```json
{
  "galleries": [
    {
      "handle": "seoyeon",        // 주소가 됩니다 → #/g/seoyeon
      "name": "김서연",            // 작품 라벨에 표시
      "title": "느린 아침",         // 전시 제목
      "theme": "night",           // night / white / concrete / deep
      "room": "grand",            // 지금은 grand 하나뿐입니다. 생략해도 같습니다
      "layout": [                 // 놓아둔 가구. 없으면 빈 배열이거나 생략
        { "t": "bench", "x": -2.6, "z": 1.4, "r": 0 }
      ],
      "guestbook": [              // 지난 방명록. 오래된 것부터
        { "name": "김서연", "msg": "색이 정말 좋다" }
      ],
      "works": [
        {
          "slot": 0,              // 벽의 자리 번호, 0~19
          "src": "works/seoyeon-01.mp4",
          "title": "창가",
          "note": "바람에 흔들리는 잎",
          "scale": 1.2            // 생략 가능, 0.6~1.4
        }
      ]
    }
  ]
}
```

전시관은 지금 하나뿐이라 `room` 은 적지 않아도 됩니다. 예전에는 10자리짜리
`hall` 이 함께 있었는데 아무도 고르지 않아 걷어냈습니다 — 그 이름이 남아
있는 옛 파일도 그대로 열립니다(모르는 이름은 기본 전시관으로 봅니다).

**큰 전시실** `grand` — 28 × 16m, 작품 20점.
네 벽을 모두 쓰고 가벽이 둘이라 동선이 두 갈래로 갈라집니다.

| 번호 | 위치 |
|---|---|
| 0 1 2 3 | 왼쪽 벽 (안쪽 → 입구 쪽) |
| 4 5 6 7 | 오른쪽 벽 |
| 8 9 10 11 12 | 정면 벽 (왼쪽 → 오른쪽) |
| 13 14 15 | 입구 쪽 벽 (문과 방명록을 피해 앉습니다) |
| 16 17 | 왼쪽 가벽 앞뒤 |
| 18 19 | 오른쪽 가벽 앞뒤 |

자리 이름은 `lib/rooms.ts` 에, 벽 좌표는 `public/exhibition.html` 의 `ROOMS`
표에 있습니다. 전시관을 새로 만들 때는 두 표를 함께 고칩니다 — 순서가
어긋나면 학생이 고른 자리와 실제로 걸리는 벽이 달라집니다.

### 가구

`layout` 은 전시장에 놓아둔 가구입니다. 한 전시장에 여덟 개까지.

| 값 | 뜻 |
|---|---|
| `t` | 종류 — `bench` `sofa` `table` `stool` `plant` `stand` |
| `x` `z` | 바닥 좌표(미터). 방 가운데가 0 이고 문 쪽이 +z 입니다 |
| `r` | 바라보는 방향(라디안). 0 이면 정면 벽을 등집니다 |

종류 이름은 `lib/furniture.ts` 에, 생김새와 부딪히는 반지름은
`public/exhibition.html` 의 `FURNITURE` 표에 있습니다. 모르는 종류는
조용히 건너뜁니다 — 손으로 고친 파일 한 줄 때문에 전시장이 안 열리지
않도록.

**빈 자리는 그냥 비워두면 됩니다.** 점선 액자로 표시돼서 아직 채우는
중이라는 게 보입니다. `works: []` 로 두면 전부 빈 전시장이 됩니다.

이미지도 걸 수 있습니다. 확장자로 알아서 구분해요.

## 작업 순서

1. 학생에게 원본 이미지와 "어떻게 움직였으면 좋겠는지" 한 줄을 받습니다
2. 선생님이 grok.com 에서 이미지를 올리고 영상을 만듭니다
3. 내려받은 mp4 를 `works/` 에 넣고 이름을 `학생-01.mp4` 식으로 정리합니다
4. `exhibition.json` 에 한 줄 추가합니다
5. 브라우저 새로고침 — 바로 반영됩니다

mp4 가 아니어도 됩니다. webm 도, 이미지도 확장자로 알아서 구분해서
겁니다. Grok 을 쓰지 않은 작품과 섞어도 됩니다.

## 학생 작업이 사라지지 않게 — 이게 가장 중요합니다

**`npm run backup` 한 번이 학기 전체의 사본입니다.**

```bash
npm run backup
```

만들어지는 폴더에는 **전시장에 걸지 않은 영상까지 전부** 들어 있습니다.

```
backup/전시-2026-08-29/
├─ students/<이름>/*.mp4    학생별 영상 전부
├─ archive.json             누가 언제 무엇을 만들었는지 · 프롬프트 · 비용
├─ exhibition.json          전시장에 걸린 작품 목록
├─ exhibition.html          전시장 · theme.css · vendor/three.min.js
└─ 읽어보세요.txt            몇 달 뒤 이 폴더를 다시 열 사람을 위해
```

**예전에는 걸린 작품만 받았습니다.** 학생이 만든 영상 대부분은 아직
걸려 있지 않습니다 — 고르는 중이거나, 자리가 열 개뿐이라서요. 그것들은
백업에 담기지 않았고, Supabase 가 멈추면 그대로 사라졌습니다.

**언제 돌려야 하나.** 무료 플랜은 오래 놀리면 프로젝트를 재우고, 저장
1GB 를 넘겨도 막힙니다. 수업이 있는 주의 끝, 그리고 방학 전에는 꼭
한 번 돌려서 폴더를 다른 곳(외장 디스크·학교 드라이브)에 복사해 두세요.

**같은 폴더로 다시 돌리면 이미 받은 것은 건너뜁니다.** 매주 돌려도
새로 만든 것만 더해집니다.

### 자동으로 돌리지 않는 이유

예약해 두면 편할 것 같지만 권하지 않습니다. 선생님 노트북에서 도는
예약 작업은 조용히 죽습니다 — 노트북이 닫혀 있거나, 드라이브 로그인이
풀렸거나, 학교 망이 막았거나. **그런데 그 로그를 아무도 보지 않습니다.**
"매주 돌고 있겠지" 하고 지내다가 정작 필요할 때 석 달 전 백업을
발견하는 것이 가장 나쁩니다. 손으로 하면 적어도 돌렸는지는 압니다.

대신 손으로 하는 것의 약점은 잊는 것입니다. 그래서 **받을 것이 몇 편
남았는지** 를 한 줄로 볼 수 있게 해뒀습니다. 받지는 않습니다.

```bash
npm run backup -- --check
```

```
  저장 위치   G:\내 드라이브\ArtGallery\전시-2026-08-29
  이미 받은 것 6편
  아직 없는 것 2편  ← npm run backup 을 돌리세요
```

**"백업하세요" 라는 알림은 무시되지만 "안 받은 게 12편입니다" 는
무시하기 어렵습니다.** 수업 시작할 때 이 한 줄만 보세요. 숫자가 0 이
아니면 그때 `npm run backup` 을 돌리면 됩니다.

주마다 잊지 않으려면 윈도우 **작업 스케줄러**에 백업이 아니라 **알림**을
거세요(월요일 아침에 메시지 띄우기). 백업 자체는 사람이 눌러야
"안 돌았다" 는 것도 사람이 압니다.

### 어디에 받을까 — 로컬 · 구글 드라이브

받는 곳은 세 군데 중 하나로 정합니다. 찾는 순서는
`--out` 인자 → `.env.local` 의 `BACKUP_DIR` → 프로젝트의 `backup/` 입니다.

```bash
npm run backup                                   backup/전시-날짜
npm run backup -- --out "D:\전시학기"          이번만 다른 곳에
```

늘 같은 곳에 두려면 `.env.local` 에 한 줄 적어두세요. 한글과 공백이
들어간 경로도 그대로 됩니다.

```
BACKUP_DIR=D:\전시보관
```

**구글 드라이브(내 드라이브)로 바로 보내려면** — 가장 쉬운 길입니다.

1. **구글 드라이브 데스크톱**을 설치하고 로그인합니다
   (https://www.google.com/drive/download — 무료)
2. 내 드라이브가 탐색기에 드라이브 문자로 붙습니다. 보통 `G:` 입니다
3. `.env.local` 에 그 안의 폴더를 적습니다

```
BACKUP_DIR=G:\내 드라이브\전시보관
```

이제 `npm run backup` 을 돌리면 **받는 즉시 드라이브로 올라갑니다.**
앱에 구글 계정을 연결하지 않아도 되고, 코드도 건드리지 않습니다.
드라이브 데스크톱이 꺼져 있으면 저장할 곳이 없다고 알려주고 멈춥니다.

**용량을 보세요.** 5초 480p 한 편이 3MB 안팎입니다. 30명이 10편이면
약 1GB 이고, 구글 무료 계정은 15GB 입니다. 매주 새 날짜 폴더가 쌓이니
학기가 끝나면 오래된 폴더는 지우거나, 늘 같은 폴더에 받도록
`--out` 으로 한 곳을 지정해 덮어 쓰세요(이미 받은 파일은 건너뜁니다).

**드라이브 데스크톱을 깔 수 없다면** `rclone` 이라는 무료 도구로도
됩니다. 한 번 `rclone config` 로 구글 계정을 연결한 뒤

```bash
npm run backup
rclone copy backup "gdrive:전시보관" --progress
```

앱이 구글 자격 증명을 들고 있지 않아도 된다는 점은 같습니다.

`SUPABASE_SERVICE_ROLE_KEY` 가 있어야 전원의 것을 받습니다. 없으면
공개 전시장에 걸린 것만 받고 **그렇다고 화면에 알려줍니다** — 조용히
절반만 받아두면 백업이 있다고 믿게 되는 쪽이 더 위험합니다.

## 폴더를 자동으로 만들기

Supabase 를 쓰고 계신다면 손으로 옮길 필요가 없습니다.

```bash
npm run backup
```

전시장 목록과 영상을 전부 내려받아 `backup/전시-날짜/` 폴더를 만듭니다.
`--out D:\전시` 로 위치를 지정할 수도 있습니다.

**three.js 도 같이 받아서 `vendor/` 에 넣고 태그를 상대 경로로 바꿉니다.**
이게 중요합니다 — `public/exhibition.html` 은 three.js 를 CDN 에서 받기
때문에, 그냥 복사만 하면 인터넷 없는 자리에서 화면이 통째로 비어 버립니다.

전시장 화면의 **"내 전시 백업"** 은 목록(`exhibition.json`)만 내려받습니다.
그 안의 주소는 아직 인터넷 주소라, 영상까지 손에 쥐려면 위 명령을 쓰세요.
관리자에게는 **"전체 전시 백업"** 이 함께 보입니다.

## 전시 당일

**노트북 한 대와 이 폴더만 있으면 됩니다.** 인터넷도 필요 없습니다 —
단, `npm run backup` 으로 만든 폴더여야 합니다. 위 문단을 보세요.

프로젝터나 큰 모니터에 띄우고 관람객이 직접 걸어다니게 하거나,
학생들 각자 노트북에서 열게 해도 됩니다.

백업에 담겨 온 **지난 방명록은 벽에 그대로 걸립니다.** 다만 당일 그 자리에서
새로 남긴 글은 메모리에만 있어서 새로고침하면 사라집니다 — 파일만 있는
자리에는 저장할 곳이 없습니다. 당일 방명록까지 남기시려면 Supabase 단계로
가세요(그쪽은 로그인 없이도 남긴 글이 저장됩니다).

## 인터넷에 올리기

폴더째로 정적 호스팅에 올리면 링크로 공유됩니다.
Netlify 는 폴더를 창에 끌어다 놓기만 하면 되고, 무료입니다.

영상이 많으면 용량을 확인하세요. 720p 8초 mp4 가 대략 3~6MB 라,
100편이면 300~600MB 입니다. 무료 한도를 넘을 수 있습니다.

## 이 방식의 한계

- 영상 생성을 선생님이 전부 손으로 합니다
- 학생별 한도 관리가 없습니다
- 당일 남긴 방명록이 저장되지 않습니다 (지난 것은 백업에 담겨 보입니다)
- 학생이 스스로 전시장을 만들 수 없습니다

이 중 하나라도 걸리기 시작하면 Supabase 단계로 넘어갈 때입니다.
그때 `README.md` 를 보세요.
